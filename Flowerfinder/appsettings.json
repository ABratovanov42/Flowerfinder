{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source=flowerapp.db"
  },
    "PlantNet": {
        "_help": "Photo identification. Get a free key at https://my.plantnet.org (Settings -> API) and put it in appsettings.Local.json, not here — this file is committed to git. 'demo' returns a canned result so the page works without a key; empty disables the feature.",
        "ApiKey": "demo"
    },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
